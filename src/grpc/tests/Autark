cc {
  set { _
    grpc_test_client1.c
  }
  ${CFLAGS_TESTS}
}

foreach {
  OBJ
  ${CC_OBJS}
  run {
    exec { ${CC} ${OBJ} ${LDFLAGS_TEST} -o %{${OBJ}} }
    consumes { ${LIBIWNET_A} ${OBJ} }
    produces { %{${OBJ}} }
  }
}

#if { ${IWNET_RUN_TESTS}
#
#}
