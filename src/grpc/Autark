set {
  parent {
    SOURCES
  }
  ..${SOURCES}
  grpc/iwn_grpc_client.c
  grpc/iwn_grpc.c
}

set {
  parent {
    PUB_HDRS
  }
  ..${PUB_HDRS}
  grpc/iwn_grpc_client.h
}

set {
  root {
    CFLAGS
  }
  ..${CFLAGS}
  -I SS{}
}

if { ${IWNET_BUILD_TESTS}
  include { tests/Autark }
}
